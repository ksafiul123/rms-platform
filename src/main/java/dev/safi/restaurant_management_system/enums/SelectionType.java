package dev.safi.restaurant_management_system.enums;

public enum SelectionType {
    SINGLE,      // Radio buttons (choose one)
    MULTIPLE,    // Checkboxes (choose multiple)
    QUANTITY     // Input field (enter quantity)
}
